import { greetings } from './greetings';

// 1️⃣ Write a method greetings(name) that interpolates name in a simple greeting.

describe('', () => {
  it('', () => {
    expect(greetings()).toBe('Hello World');
  });
});
